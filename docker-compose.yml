version: '3.8'

services:
  web:
    build: .
    container_name: agenda_medica_app
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./database.db:/app/database.db
    restart: always